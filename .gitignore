build/
*.o
*.so
*.dylib
*.dll
# dist/ holds the committed prebuilt library — keep it tracked
!dist/*.so
!dist/*.dylib
!dist/*.dll
__pycache__/
*.pyc
# personal dev notes / session resume command — never push
DEV_HISTORY.md
